//Expression = Piece of code that produces a value
3 + 4;
1995;
true && false && !false;

//Statements Doesn't necissarily produce a value.
if (23 > 10) {
    const str = '23 is bigger'; //String itself is a expression
}

const me = 'Matt';
console.log(`I'm ${2037 - 1995} years old ${me}`);